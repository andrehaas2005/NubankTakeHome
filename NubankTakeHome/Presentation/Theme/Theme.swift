import UIKit

enum Theme {
  
  /// Paleta de cores
  enum Colors {}
  
  /// Tipografia
  enum Typography {}
  
  /// Espaçamentos / layout tokens
  enum Spacing {}
  
  /// Bordas arredondadas
  enum Radii {}
  
  /// Sombras
  enum Shadows {}
  
  /// Utilidades de estilo
  enum Utils {}
}

