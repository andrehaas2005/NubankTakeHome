import UIKit

extension Theme.Shadows {
  
  static let micro: (opacity: Float, radius: CGFloat, offset: CGSize) = (
    opacity: 0.08,
    radius: 2,
    offset: CGSize(width: 0, height: 1)
  )
  
  static let small: (opacity: Float, radius: CGFloat, offset: CGSize) = (
    opacity: 0.12,
    radius: 4,
    offset: CGSize(width: 0, height: 2)
  )
  
  static let medium: (opacity: Float, radius: CGFloat, offset: CGSize) = (
    opacity: 0.15,
    radius: 8,
    offset: CGSize(width: 0, height: 4)
  )
}
